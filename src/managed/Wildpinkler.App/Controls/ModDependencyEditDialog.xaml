<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Wildpinkler.App.Controls.ModDependencyEditDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Dependencies"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    Style="{StaticResource DefaultContentDialogStyle}">

    <ScrollViewer MaxHeight="480" Padding="0,0,16,0" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="20">
            <StackPanel Spacing="8">
                <TextBlock Style="{StaticResource SectionLabelStyle}" Text="CURRENT" />
                <ListView x:Name="DependencyList" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="8" Padding="0,4,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" VerticalAlignment="Center" />
                                <Button Grid.Column="1" Content="Remove" Click="RemoveDependency_Click"
                                        AutomationProperties.Name="Remove dependency" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock x:Name="EmptyText" Opacity="0.68" Text="No dependencies recorded yet." Visibility="Collapsed" />
            </StackPanel>

            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" />

            <StackPanel Spacing="12">
                <TextBlock Style="{StaticResource SectionLabelStyle}" Text="ADD DEPENDENCY" />
                <ComboBox x:Name="KindBox" Header="Kind" SelectionChanged="Kind_SelectionChanged" HorizontalAlignment="Stretch">
                    <x:String>Requires</x:String>
                    <x:String>LoadAfter</x:String>
                    <x:String>LoadBefore</x:String>
                    <x:String>Conflicts</x:String>
                    <x:String>GameVersion</x:String>
                </ComboBox>

                <ComboBox x:Name="TargetModBox" Header="Target mod" HorizontalAlignment="Stretch"
                          DisplayMemberPath="Name" Visibility="Collapsed" />

                <StackPanel x:Name="GameVersionFields" Spacing="12" Visibility="Collapsed">
                    <TextBox x:Name="ExactVersionBox" Header="Required game version (e.g. 1.6.640.0)" />
                    <TextBox x:Name="VersionLabelBox" Header="Label shown to the user (e.g. Skyrim SE 1.6.640)" />
                </StackPanel>

                <Button x:Name="AddButton" Content="Add" Click="AddDependency_Click" HorizontalAlignment="Left" />
                <TextBlock x:Name="AddErrorText" Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                           TextWrapping="Wrap" Visibility="Collapsed" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
